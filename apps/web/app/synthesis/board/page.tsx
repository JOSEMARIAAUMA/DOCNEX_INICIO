import SynthesisBoard from '@/components/synthesis/SynthesisBoard';

export default function SynthesisPage() {
    return (
        <div className="h-screen w-full bg-background overflow-hidden">
            <SynthesisBoard />
        </div>
    );
}
